export interface CompileSettings {
    packageDir: string;
    namedAddresses: string;
    moveVersion: string;
    optimizer: boolean;
    optimizerlevel: string;
    bytecodeHash: string;
    network: string;
}